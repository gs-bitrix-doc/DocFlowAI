# Backlog

## 03.04.2026

### Что сделали

**Протестировали пайплайн на новом файле `entity-item-get.md`:**
Первый запуск выявил три структурных бага — устранили все три:

| Баг | Причина | Фикс |
|---|---|---|
| YFM-заголовок таблицы (`Название / тип`) дублировался | LLM схлопывал строки | Статическая замена в `PreTranslator._TABLE_HEADER_PATTERN` |
| Лейблы вкладок переименовывались (`- JS` → `* JavaScript`) | Protector не защищал лейблы `{% list tabs %}` | Новый паттерн `_TAB_LABEL_PATTERN` в Protector |
| NBSP (`\u00a0`) вместо пробелов в переводе | LLM вставлял невидимые символы | Замена `\u00a0 → ' '` в `Translator.translate()` |

**Исправили ложный позитив валидатора:**
Substring-поиск `«лид» in «валидатора»` вызывал ложное срабатывание. Заменили на `re.search` с `\b` — теперь только точное совпадение по границам слова.

**Интегрировали внешний глоссарий (903 термина):**
Получили CSV-файл от команды. Написали скрипт конвертации, результат сохранили как `data/glossary.json`. Реализовали архитектуру двух словарей:
- `dictionary.json` — попадает в промпт LLM + используется валидатором
- `glossary.json` — только для валидатора, в промпт не попадает (экономия токенов)

Передача в пайплайн: `config.load_glossary()` → `pipeline.run(glossary=...)` → `StructureValidator.validate(combined)`.

**Очистили промпт от дублирования:**
`data/prompt.txt` — раздел `## Terminology` содержал термины, которые уже попадали в промпт через Glossary-секцию. Удалили дублирующие записи, оставили только 9 правил с важным контекстом или исключениями. Промпт сократился с ~5600 до 4636 символов.
Добавлен новый раздел `## Section headings style` (noun phrases, Тип X → X type).

**Исправили проблемные записи в `glossary.json`:**
- Удалена запись `"ID": "ID"` — вызывала ложные срабатывания валидатора (идентичные стороны)
- Исправлена `"Символьный код": "Mnemonic code"` → `"symbolic code"` (устаревший перевод в исходном CSV)

**Добавили переводы `элемент` и `Продолжите изучение` в PreTranslator:**
LLM игнорировал словарь и переводил `элемент` → `element` вместо `item`. Решение — добавить все 8 падежных форм в `STATIC_TERMS`, замена происходит до LLM. Аналогично добавили `"Продолжите изучение" → "Continue learning"` в новый словарь `SECTION_HEADINGS`.

**Рефакторинг PreTranslator — словари вынесены в JSON-файлы:**
Все четыре словаря (`INCLUDE_LABELS`, `NOTE_TITLES`, `SECTION_HEADINGS`, `STATIC_TERMS`) перенесены из Python-файла в `data/pre_translator/*.json`. `pre_translator.py` загружает их при старте. Добавлять новые записи теперь можно без изменения кода.

**Протестировали `bitrixgpt-5.5` после всех правок:**
Качество подтянулось вплотную к `gpt-oss-120b`. Оставшиеся расхождения — уровень стилистики, не терминологии. Результаты зафиксированы в `TRANSLATION_ANALYSIS.md`.

---

## 02.04.2026

### Что сделали

**Подключили реальный API и провели первый тест:**
Получили ключ Bitrix GPT (LiteLLM). Исправили имя модели в `.env` (`gpt-oss-120b`). Успешно перевели `crm-deal-get.md` — структура сохранена, текст переведён корректно.

**Добавили PreTranslator — статичные замены до LLM:**
Новый модуль `src/processors/pre_translator.py` выполняет замены до того, как Protector скрывает текст от модели:
- Метки include-блоков (`[Сноска о параметрах]` → `[Note on parameters]`)
- Заголовки битрикс-нот (`"Развитие метода остановлено"` → `"Method development has been halted"`)
- `` `тип` `` → `` `type` ``
- `![alt-текст]` → `![alt text]`

**Добавили CodeTranslator:**
Новый модуль `src/processors/code_translator.py` — отдельный LLM-вызов до `protect()`. Находит кириллические строковые значения внутри кодблоков, батчит их в один запрос, переводит и подставляет обратно. Код-блоки при этом по-прежнему защищаются целиком — структура не страдает.

**Добавили статичные замены нестабильных терминов в PreTranslator:**
`смарт-процесс` (во всех падежах) → `SPA`/`SPAs` — LLM не всегда следовал промпту, статика надёжнее.

**Обновили промпт и словарь терминов:**
Синхронизировали с промптом production-сервиса перевода (Сергей Востриков).
Исправлены неверные переводы: `портал` → `account`, `коммерческое предложение` → `estimate`, `смарт-процесс` → `SPA`.
Добавлены правила: замена имён, телефонов (+7→+1), городов, часовых поясов, локалей (ru→de), доменов.
Удаление упоминаний GigaChat, YandexGPT, Sber, Yandex.
Явное правило: `сделка` = `deal`, никогда не `activity`.

**Обновили валидатор:**
Теперь проверяет кириллицу во всём файле включая кодблоки (раньше исключали).

**Провели сравнительное тестирование моделей на `crm-deal-get.md`:**
`gpt-oss-120b` показал более точное следование словарю и промпту. `bitrixgpt-5.5` быстрее, но игнорировал часть терминологии. Результаты зафиксированы в `TRANSLATION_ANALYSIS.md`.

**Задокументировали результаты тестов:**
Создан `TRANSLATION_ANALYSIS.md` — разбор качества перевода по каждому прогону, найденные проблемы и выводы.

---

## 26.03.2026

### Что сделали

**Настроили проект:**
Создали структуру папок, установили зависимости через `uv`, настроили виртуальное окружение.

**Реализовали пайплайн перевода:**
Написали пять модулей которые работают последовательно — читают файл, защищают структуру, переводят, проверяют результат и сохраняют.

- `parser.py` — читает `.md` файл, проверяет что файл существует и имеет правильное расширение
- `protector.py` — защищает элементы которые нельзя переводить: блоки кода, ссылки, битрикс-теги, названия полей API, якоря. Заменяет их на плейсхолдеры `[[PROTECTED_N]]` и восстанавливает обратно после перевода
- `translator.py` — отправляет текст в Bitrix GPT и возвращает перевод
- `validator.py` — сравнивает структуру оригинала и перевода: считает заголовки, таблицы, блоки кода, проверяет что все термины из словаря переведены
- `writer.py` — сохраняет результат в папку `output/` с суффиксом `.en.md`

**Настроили конфигурацию:**
Вынесли промпт в отдельный файл `prompt.txt` и словарь терминов в `dictionary.json` — оба редактируются без изменения кода. API ключи хранятся в `.env`.

**Сделали CLI:**
Скрипт запускается из командной строки. Поддерживает перевод одного файла и целой папки, а также режим `--dry-run` для проверки пайплайна без обращения к API.

**Добавили логирование:**
Каждый шаг пишется в консоль и в файл в папке `logs/` с временной меткой.

---

## Следующие шаги

- [ ] Протестировать на большем количестве файлов документации
- [ ] `DictionaryFixer` — автозамена непереведённых терминов через словарь post-LLM
- [ ] `DockerValidator` — проверка что документация собирается после перевода